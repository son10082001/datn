import { type Adapter, type Runtime } from "./utils/remix";
declare type Options = {
    runtime: Runtime;
    adapter?: Adapter;
};
declare const _default: (options: Options) => (code: string, filepath: string) => string;
export default _default;
