import { Scope } from '@sentry/core';
import { Client, ReplayEvent } from '@sentry/types';
export declare function getReplayEvent({ client, scope, replayId: event_id, event, }: {
    client: Client;
    scope: Scope;
    replayId: string;
    event: ReplayEvent;
}): Promise<ReplayEvent | null>;
//# sourceMappingURL=getReplayEvent.d.ts.map